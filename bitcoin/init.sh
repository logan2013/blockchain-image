# this is bitcoin test network
bitcoind  -conf=/root/.bitcoin/bitcoin.conf -datadir=/root/data -testnet -daemon
#this is reg self of network
#bitcoind -conf=/root/.bitcoin/bitcoin.conf -datadir=/root/data -regtest -daemon
